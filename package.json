{
  "name": "playwright-crm-automation",
  "version": "1.0.0",
  "description": "Playwright automation tests for Guardian Capital CRM",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.1",
    "dotenv": "^16.3.1"
  }
}